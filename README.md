# Vision Moderation API

Base64 image fields use placeholder strings in example payloads and represent images smaller than the 5 MB maximum upload limit.

Validation command:

```bash
redocly.cmd lint openapi.yaml
```
